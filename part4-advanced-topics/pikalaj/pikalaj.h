/*
EXERCISE: Quicksort
Write a function that implements the quick sort algorithm to sort an array of
integers. The function should take three parameters: the starting index, the
ending index, and the array to be sorted. The function should sort the elements
of the array in place using the quick sort algorithm with recursive 3-way
partitioning. The function should not return any value, as the sorting is done
in place.
*/

#ifndef PIKALAJ_H
#define PIKALAJ_H

void pikalajittele(int start, int end, int t[]);

#endif
