/*
EXERCISE: Date difference
Write a function that calculates the difference in days between two dates. The
function should take two strings as input, each representing a date in the
format "DD.MM.YYYY". The function should return the absolute difference in days
between the two dates. You can assume that the input dates are valid and that
the year is at least 1900.
*/

#ifndef PVM_ERO_H
#define PVM_ERO_H

void pvm_ero(const char *pvm1, const char *pvm2);

#endif
